# GrowPal 后端环境配置说明

## 当前配置状态
✅ 数据库配置已就绪
✅ JWT密钥已配置
❌ **LLM API 密钥未配置** - 这是AI聊天功能无法使用的原因

## AI聊天功能配置步骤

### 选项1: 使用 OpenAI API (推荐)
1. 前往 https://platform.openai.com 注册账号
2. 获取 API Key
3. 在下方的 LLM_API_KEY 填入你的密钥

### 选项2: 使用兼容 OpenAI 接口的国内服务
例如: 通义千问、智谱AI、Moonshot等
1. 获取对应服务的 API Key
2. 修改 LLM_API_BASE 为对应的API地址
3. 设置 LLM_MODEL 为对应的模型名称

### 选项3: 使用 Ollama 本地部署
1. 安装 Ollama: https://ollama.com
2. 拉取模型: `ollama pull qwen2.5`
3. 设置 LLM_API_BASE=http://localhost:11434/v1
4. 设置 LLM_MODEL=qwen2.5
5. LLM_API_KEY 可以留空或随便填

## 配置示例 (取消注释并填入你的密钥)

LLM_API_KEY=sk-your-api-key-here
LLM_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7

## 测试配置
配置完成后,重启后端服务:
```bash
cd server
node index.js
```

然后在前端尝试发送一条AI消息,如果配置正确,应该能收到回复。
